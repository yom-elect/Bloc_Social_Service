paths:
  /api/v1/blocs/blocName:
    get:
      tags:
        - "bloc"
      summary: "Get a bloc by its name"
      produces:
        - "application/json"
      parameters:
        - name: "blocName"
          in: "query"
          required: true
          type: "string"
      responses:
        "200":
          description: "successful operation"
      security:
        - bearerAuth: []