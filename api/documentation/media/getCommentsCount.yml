paths:
  /api/v1/media/{mediaId}/comments/count:
    get:
      tags:
        - "media"
      summary: "Get a media's comments count"
      produces:
        - "application/json"
      parameters:
        - name: "mediaId"
          in: "path"
          required: true
      responses:
        "200":
          description: "successful operation"
      security:
        - bearerAuth: []