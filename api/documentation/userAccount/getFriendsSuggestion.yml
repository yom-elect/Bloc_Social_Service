paths:
  /api/v1/users/me/friendsSuggestion:
    get:
      tags:
        - "userAccount"
      summary: "Get friends suggestion for a user"
      produces:
        - "application/json"
      parameters:
        - name: "page"
          in: "query"
          required: true
          type: "string"
      responses:
        "200":
          description: "successful operation"
      security:
        - bearerAuth: []