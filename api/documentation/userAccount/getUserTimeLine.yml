paths:
  /api/v1/users/me/timeline:
    get:
      tags:
        - "userAccount"
      summary: "Get user Timeline/Feed"
      produces:
        - "application/json"
      parameters:
        - name: "page"
          in: "path"
          required: false
      responses:
        "200":
          description: "successful operation"
      security:
        - bearerAuth: []