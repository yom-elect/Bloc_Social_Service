paths:
  /api/v1/profile/me/rank/:
    get:
      tags:
        - "userProfile"
      summary: "Get a user rank/point from all their activities in the application"
      produces:
        - "application/json"
      # parameters:
      #   - name: "username"
      #     in: "path"
      #     required: true
      responses:
        "200":
          description: "successful operation"
      security:
        - bearerAuth: []